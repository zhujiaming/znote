import 'package:flutter/material.dart';

void showGitLoginDialog(BuildContext context){
  showDialog(
      context: context,
      builder: (context) {
        return SimpleDialog(
          children: [

          ],
        );
      });
}